{
  "name": "quizit",
  "private": true,
  "description": "Dev-only test tooling for QuizIt. The site itself has no build step or dependencies.",
  "scripts": {
    "test": "npm run test:data && npm run test:e2e",
    "test:data": "node --test tests/data/*.test.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
